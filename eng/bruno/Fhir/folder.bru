meta {
  name: Fhir
  seq: 2
}

docs {
  The FHIR REST API: resource CRUD and search (`Resources`), instance-level
  requests, operations, validation and the capability statement. All requests
  target `{{fhirBase}}` and work against any of the configured servers.
}
