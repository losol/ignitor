meta {
  name: Meta (read)
  type: http
  seq: 2
}

post {
  url: {{fhirBase}}/Patient/{{patientId}}/$meta
  body: json
  auth: inherit
}

headers {
  Content-Type: application/fhir+json
}

body:json {
  {
    "resourceType": "Parameters"
  }
}

docs {
  Instance-level `$meta` operation — returns the resource's `meta` (tags,
  profiles, security labels) as a `Parameters` resource. The body is an empty
  `Parameters`. Uses `patientId` from **Create Patient**.
}
