meta {
  name: Patient
}

docs {
  Success and failure examples for `Patient` request bodies. Failure responses
  are `400 Bad Request` with an `OperationOutcome`; element-level errors carry
  one issue per invalid element, each with a FHIRPath `expression`
  (e.g. `Patient.gender`).
}
