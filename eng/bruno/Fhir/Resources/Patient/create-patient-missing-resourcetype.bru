meta {
  name: Create Patient (missing resourceType)
  type: http
  seq: 5
}

post {
  url: {{fhirBase}}/Patient
  body: json
  auth: inherit
}

headers {
  Content-Type: application/fhir+json
}

body:json {
  {
    "name": [
      {
        "family": "Losvik"
      }
    ]
  }
}

assert {
  res.status: eq 400
  res.body.resourceType: eq OperationOutcome
}

docs {
  Valid JSON, but not a FHIR resource: no `resourceType` property. Expect `400`
  with an issue like *Resource has no 'resourceType' property*
  (`code: structure`).
}
