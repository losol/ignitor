meta {
  name: Task
}

docs {
  Create, update and search examples for `Task`, tying together the patient
  and practitioner from the other folders — run **Patient → Create Patient
  (valid)** and **Practitioner → Create Practitioner (valid)** first so
  `patientId` and `practitionerId` are set.
}
