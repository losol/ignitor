meta {
  name: Observation
}

docs {
  Create and search examples for `Observation`, referencing the patient from
  **Patient → Create Patient (valid)** (run that first so `patientId` is set).
  Includes invalid coded value and invalid quantity examples.
}
