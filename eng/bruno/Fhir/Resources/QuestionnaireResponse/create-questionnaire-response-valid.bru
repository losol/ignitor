meta {
  name: Create QuestionnaireResponse (valid)
  type: http
  seq: 1
}

post {
  url: {{fhirBase}}/QuestionnaireResponse
  body: json
  auth: inherit
}

headers {
  Content-Type: application/fhir+json
}

body:json {
  {
    "resourceType": "QuestionnaireResponse",
    "status": "completed",
    "subject": {
      "reference": "Patient/{{patientId}}"
    },
    "item": [
      {
        "linkId": "q1",
        "answer": [
          {
            "valueBoolean": false
          }
        ]
      },
      {
        "linkId": "q2",
        "answer": [
          {
            "valueString": "Penicillin"
          }
        ]
      }
    ]
  }
}

assert {
  res.status: eq 201
  res.body.resourceType: eq QuestionnaireResponse
}

docs {
  Expect `201 Created`. The `questionnaire` element is omitted on purpose —
  its type differs between FHIR versions (Reference in STU3, canonical from
  R4), so leaving it out keeps the example valid against any of the servers.
}
