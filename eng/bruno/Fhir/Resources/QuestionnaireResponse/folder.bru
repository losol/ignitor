meta {
  name: QuestionnaireResponse
}

docs {
  Create examples for `QuestionnaireResponse`, answering the questionnaire
  from **Questionnaire → Create Questionnaire (valid)** on behalf of the
  patient from **Patient → Create Patient (valid)**. Run both first.
}
