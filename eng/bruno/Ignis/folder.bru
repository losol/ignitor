meta {
  name: Ignis
  seq: 1
}

docs {
  Server operations and administration: authentication (`Auth → Get token` —
  run this first), archive import, maintenance ($clear-store, $rebuild-index)
  and the health endpoint.
}
